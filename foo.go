// Package gopathonly exists only in the gopath. It is not vendored into the
// main package. Here are some docs for it.
package gopathonly

// Foo returns the string "foo"
func Foo() string {
	return "foo"
}

// Bar returns the string "bar"
func Bar() string {
	return "bar"
}
